name: CI Build Ubuntu
on: push
jobs:
  check_commit:
    uses: alfeilex/alfeilex.github.io/.github/workflows/check-commit.yml@master
  build:
    runs-on: ubuntu-latest
    needs: check_commit
    if: ${{ needs.check_commit.outputs.should_run != 'false' }}
    steps:
      - name: test
        uses: actions/checkout@v3
        with:
          submodules: recursive
      - name: set an output to test
        id: Test1
        run: echo "test"
      
